apiVersion: 1

# Root policy: every alert in this org routes to the single contact point
# above. No per-severity routing — not worth the complexity for 4 rules.
policies:
  - orgId: 1
    receiver: resend-email
    group_by: ["alertname"]
